# veil

Midnight Network application — local setup and scripts are documented in `README.md`.

## Design

@DESIGN.md

## Agent skills

### Issue tracker

GitHub Issues on this repository using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles map one-to-one to GitHub label names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: domain glossary in `CONTEXT.md` and architecture notes in `docs/adr/` at the repo root (create or extend those files as the project evolves). See `docs/agents/domain.md`.
